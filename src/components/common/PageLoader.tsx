import type { FC } from "react";

const PageLoader: FC = () => {
    return (
        <div className="flex min-h-40 items-center justify-center p-4 text-xs text-gray-400 sm:text-sm">
            Yuklanmoqda...
        </div>
    );
};

export default PageLoader;